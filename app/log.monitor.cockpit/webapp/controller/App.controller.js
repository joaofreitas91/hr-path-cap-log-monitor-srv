sap.ui.define([
  "sap/ui/core/mvc/Controller"
], (BaseController) => {
  "use strict";

  return BaseController.extend("com.hrpath.log.monitor.cockpit.controller.App", {
      onInit() {
      }
  });
});